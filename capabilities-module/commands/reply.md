Draft a reply to the message below.
Write it in this agent's voice — read CLAUDE.md (and PERSONA.md if present).
Output ONLY the reply text — no preamble. If a fact is missing, use [bracketed placeholder].

Message to reply to:
$ARGUMENTS